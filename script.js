function showSidebar() {
    const sideIcon = document.querySelector(".sidebar-icon");
    const sideLinks = document.querySelector(".sidebar-links");

    sideIcon.classList.toggle("open");
    sideLinks.classList.toggle("open");
}
